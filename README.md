# A Sandbox environment for [Playwright](https://playwright.dev/) and Python
## How to use
To install the sanbox environment use:
```
make build
```

## To access a shell inside de environment run
To install the sanbox environment use:
```
make shell
```
